name: Build AyuGram Desktop

on:
  workflow_dispatch:
    inputs:
      branch:
        description: 'AyuGram branch to build'
        required: true
        default: 'dev'

permissions:
  contents: write

env:
  TDESKTOP_API_ID: "2040"
  TDESKTOP_API_HASH: "b18441a1ff607e10a989891a5462e627"

jobs:

  windows:
    name: Windows ${{ matrix.arch }}
    runs-on: windows-2022
    timeout-minutes: 360

    strategy:
      fail-fast: false
      matrix:
        arch: [x64, x86]

    defaults:
      run:
        shell: cmd

    steps:
      - name: Checkout builder repo
        uses: actions/checkout@v4

      - name: Clean disk
        run: |
          if exist "C:\Program Files\Android" rmdir /s /q "C:\Program Files\Android"
          if exist "C:\hostedtoolcache\windows" rmdir /s /q "C:\hostedtoolcache\windows"
          if exist "C:\Program Files\dotnet" rmdir /s /q "C:\Program Files\dotnet"
          if exist "C:\mysql" rmdir /s /q "C:\mysql"
          net stop SQLWriter 2>nul || ver >nul
          taskkill /F /IM sqlwriter.exe /T 2>nul || ver >nul
          taskkill /F /IM sqlservr.exe /T 2>nul || ver >nul
          if exist "C:\Program Files\Microsoft SQL Server" rmdir /s /q "C:\Program Files\Microsoft SQL Server"
          exit /b 0

      - name: Expand virtual memory
        uses: al-cheb/configure-pagefile-action@v1.4
        with:
          minimum-size: 16GB
          maximum-size: 32GB
          disk-root: "C:"

      - name: Setup environment
        shell: bash
        run: |
          REPO_NAME="${GITHUB_REPOSITORY##*/}"
          echo "REPO_NAME=$REPO_NAME" >> $GITHUB_ENV

          mkdir -p "$USERPROFILE/TBuild/Libraries"
          echo "TBUILD=$GITHUB_WORKSPACE/TBuild" >> $GITHUB_ENV
          echo "LibrariesPath=$GITHUB_WORKSPACE/TBuild/Libraries/${{ matrix.arch }}" >> $GITHUB_ENV

          SDK="10.0.26100.0"
          echo "SDK=$SDK" >> $GITHUB_ENV

          sed -i '/CMAKE_${lang}_FLAGS_DEBUG_INIT/s/${_Zi}//' \
            "$PROGRAMFILES"/CMake/share/cmake*/Modules/Platform/Windows-MSVC.cmake

          git config --global user.email "AyuGramBuilder@users.noreply.github.com"
          git config --global user.name "AyuGram CI"

      - name: Create TBuild symlink
        run: mklink /d %GITHUB_WORKSPACE%\TBuild %userprofile%\TBuild

      - name: Clone AyuGramDesktop
        run: |
          git clone --recursive --depth 1 --shallow-submodules --jobs %NUMBER_OF_PROCESSORS% ^
            --branch "${{ github.event.inputs.branch }}" --filter=blob:none ^
            https://github.com/AyuGram/AyuGramDesktop.git "%TBUILD%\%REPO_NAME%"
        shell: cmd

      - name: Read version
        shell: bash
        run: |
          VFILE="$TBUILD/$REPO_NAME/Telegram/build/version"
          VERSION=$(grep 'AppVersionStr ' "$VFILE" | awk '{ print $2 }')
          VERSION_NUM=$(grep 'AppVersion ' "$VFILE" | awk '{ print $2 }')
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "VERSION_NUM=$VERSION_NUM" >> $GITHUB_ENV

          PREP="$TBUILD/$REPO_NAME/Telegram/build/prepare/prepare.py"
          echo "$(sha256sum "$PREP" | awk '{ print $1 }')" > CACHE_KEY.txt
          echo "$SDK" >> CACHE_KEY.txt
          echo "CACHE_KEY=$(sha256sum CACHE_KEY.txt | awk '{ print $1 }')" >> $GITHUB_ENV

      - name: MSVC environment
        uses: ilammy/msvc-dev-cmd@v1.13.0
        with:
          arch: ${{ matrix.arch }}
          sdk: ${{ env.SDK }}

      - name: NuGet sources
        run: |
          nuget sources Disable -Name "Microsoft Visual Studio Offline Packages"
          nuget sources Add -Source https://api.nuget.org/v3/index.json & exit 0

      - name: ThirdParty cache
        uses: actions/cache@v5
        with:
          path: ${{ env.TBUILD }}/ThirdParty
          key: ${{ runner.OS }}-${{ matrix.arch }}-third-party-${{ env.CACHE_KEY }}
          restore-keys: ${{ runner.OS }}-${{ matrix.arch }}-third-party-

      - name: Libraries cache
        uses: actions/cache@v5
        with:
          path: |
            ${{ env.LibrariesPath }}/*
            !${{ env.LibrariesPath }}/cache_keys
            !${{ env.LibrariesPath }}/[qQ]t[_-]*
            ${{ env.LibrariesPath }}/cache_keys/*
            !${{ env.LibrariesPath }}/cache_keys/[qQ]t[_-]*
          key: ${{ runner.OS }}-${{ matrix.arch }}-libs-${{ env.CACHE_KEY }}
          restore-keys: ${{ runner.OS }}-${{ matrix.arch }}-libs-

      - name: Qt cache
        uses: actions/cache@v5
        with:
          path: |
            ${{ env.LibrariesPath }}/[qQ]t[_-]*
            ${{ env.LibrariesPath }}/cache_keys/[qQ]t[_-]*
          key: ${{ runner.OS }}-${{ matrix.arch }}-qt-${{ env.CACHE_KEY }}
          restore-keys: ${{ runner.OS }}-${{ matrix.arch }}-qt-

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Apply patches
        shell: python
        run: |
          python .github/scripts/patch_updater.py AyuGramDesktop
          import os, sys, re
          sys.stdout.reconfigure(encoding='utf-8')
          src = os.path.join(os.environ["TBUILD"], os.environ["REPO_NAME"])
          patched = {}

          def patch(path, old, new):
              full = os.path.join(src, path)
              if not os.path.exists(full):
                  return
              with open(full, "r", encoding="utf-8") as f:
                  content = f.read()
              if old in content:
                  with open(full, "w", encoding="utf-8", newline="\n") as f:
                      f.write(content.replace(old, new))
                  patched.setdefault(path, []).append(old[:60])

          def patch_re(path, pattern, new):
              full = os.path.join(src, path)
              if not os.path.exists(full):
                  return
              with open(full, "r", encoding="utf-8") as f:
                  content = f.read()
              result, n = re.subn(pattern, new, content)
              if n:
                  with open(full, "w", encoding="utf-8", newline="\n") as f:
                      f.write(result)
                  patched.setdefault(path, []).append(f"regex:{pattern[:40]}")

          patch("cmake/options_win.cmake", "/MP     # Enable multi process build.", "/MP\n        /FS")
          patch_re("cmake/options_win.cmake", r'/DEBUG(?!\:NONE)', '/DEBUG:NONE')
          patch("cmake/variables.cmake",
                'set(CMAKE_MSVC_DEBUG_INFORMATION_FORMAT "ProgramDatabase" CACHE STRING "")',
                'set(CMAKE_MSVC_DEBUG_INFORMATION_FORMAT "" CACHE STRING "" FORCE)')

          p = "Telegram/build/prepare/prepare.py"
          patch(p, "qt = os.environ.get('QT')", "qt = os.environ['QT']")
          patch(p, 'move out.dbg\\ossl_static.pdb', 'if exist out.dbg echo skip')
          patch(p, 'move out.dbg\\ossl_static', 'if exist out.dbg echo skip')
          patch(p, 'SET CONFIGURATIONS=-debug-and-release', 'SET CONFIGURATIONS=-release')
          patch(p, 'CONFIGURATIONS=-debug-and-release', 'CONFIGURATIONS=-release')
          patch(p, 'cmake --build . --config Debug', 'echo skip')
          patch(p, 'cmake --build out --config Debug', 'echo skip')
          patch(p, 'cmake --install . --config Debug', 'echo skip')

          repo = os.environ.get("GITHUB_REPOSITORY", "RezoxP/AyuGramDesktop-builder")
          url = f"https://raw.githubusercontent.com/{repo}/update-manifest/"

          update_url_patterns = [
              ('return AutoupdatePrefix("https://update.ayugram.one/");',
               f'return AutoupdatePrefix("{url}");'),
              ('const auto fixedPrefix = QString::fromStdString("https://update.ayugram.one/");',
               f'const auto fixedPrefix = QString::fromStdString("{url}");'),
          ]
          t_rel = os.path.join("Telegram", "SourceFiles", "storage", "localstorage.cpp")
          for old, new in update_url_patterns:
              patch(t_rel, old, new)

          u_rel = os.path.join("Telegram", "SourceFiles", "core", "update_checker.cpp")
          patch(u_rel,
                'Local::readAutoupdatePrefix() + bestLink)',
                'bestLink.startsWith(QLatin1String("http")) ? bestLink : (Local::readAutoupdatePrefix() + bestLink))')

          required = {
              os.path.join(src, t_rel): 'AutoupdatePrefix',
              os.path.join(src, u_rel): 'bestLink.startsWith',
          }
          for fpath, marker in required.items():
              with open(fpath, "r", encoding="utf-8") as f:
                  if marker not in f.read():
                      print(f"FATAL: patch verification failed for {fpath} (missing {marker})")
                      sys.exit(1)

          for path, items in patched.items():
              print(f"Patched {path}: {len(items)} replacement(s)")

      - name: Build libraries
        env:
          GYP_MSVS_OVERRIDE_PATH: 'C:\Program Files\Microsoft Visual Studio\2022\Enterprise\'
          GYP_MSVS_VERSION: 2022
        run: |
          %TBUILD%\%REPO_NAME%\Telegram\build\prepare\win.bat silent

      - name: Free disk space
        shell: bash
        run: |
          if [ -d "$LibrariesPath" ]; then
            find $LibrariesPath \
              '(' \
                '(' \
                  ! '(' \
                    -name '*.lib' -o -name '*.a' -o -name '*.exe' \
                    -o -name '*.h' -o -name '*.hpp' -o -name '*.inc' \
                    -o -name '*.cmake' -o -path '*/include/*' \
                    -o -path '*/objects-*' -o -path '*/cache_keys/*' \
                    -o -path '*/patches/*' \
                  ')' \
                  -type f \
                ')' \
                -o -empty \
              ')' -delete
          fi

      - name: Build AyuGram
        run: |
          cd %TBUILD%\%REPO_NAME%\Telegram
          call configure.bat ^
            ${{ matrix.arch }} ^
            -D TDESKTOP_API_ID=%TDESKTOP_API_ID% ^
            -D TDESKTOP_API_HASH=%TDESKTOP_API_HASH% ^
            -D CMAKE_CONFIGURATION_TYPES=Release ^
            -D DESKTOP_APP_DISABLE_AUTOUPDATE=OFF ^
            -D DESKTOP_APP_DISABLE_CRASH_REPORTS=ON ^
            -D DESKTOP_APP_ENABLE_LTO=ON
          cmake --build ..\out --config Release --parallel %NUMBER_OF_PROCESSORS%

      - name: Package
        shell: bash
        run: |
          OUT="$TBUILD/$REPO_NAME/out/Release"
          mkdir -p pkg
          for f in AyuGram.exe Telegram.exe; do
            [ -f "$OUT/$f" ] && cp "$OUT/$f" pkg/AyuGram.exe && break
          done
          [ -f "$OUT/Updater.exe" ] && cp "$OUT/Updater.exe" pkg/
          [ -d "$OUT/modules" ] && cp -r "$OUT/modules" pkg/
          find pkg -name '*.pdb' -o -name '*.ilk' -o -name '*.exp' | xargs rm -f 2>/dev/null || true
          cd pkg && 7z a -mx9 "../AyuGram-Windows-${{ matrix.arch }}-${VERSION}.zip" . > /dev/null

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: AyuGram-Windows-${{ matrix.arch }}-${{ env.VERSION }}
          path: "AyuGram-Windows-${{ matrix.arch }}-${{ env.VERSION }}.zip"
          retention-days: 90

  linux:
    name: Linux x64
    runs-on: ubuntu-22.04
    timeout-minutes: 360

    outputs:
      version: ${{ steps.version.outputs.version }}
      version_num: ${{ steps.version.outputs.version_num }}

    steps:
      - name: Free disk space
        run: |
          sudo rm -rf /usr/local/lib/android /usr/share/dotnet \
            /opt/ghc /usr/local/graalvm /usr/local/.ghcup \
            /usr/local/share/powershell /usr/local/share/chromium \
            /usr/local/lib/node_modules 2>/dev/null || true
          sudo docker image prune --all --force 2>/dev/null || true

      - name: Clone AyuGramDesktop and pull Docker image
        run: |
          docker pull ghcr.io/telegramdesktop/tdesktop/centos_env:latest &
          DOCKER_PID=$!
          git clone --recursive --depth 1 --shallow-submodules \
            --jobs $(nproc) --filter=blob:none \
            --branch "${{ github.event.inputs.branch }}" \
            https://github.com/AyuGram/AyuGramDesktop.git tdesktop
          wait $DOCKER_PID

      - name: Read version
        id: version
        run: |
          VFILE="tdesktop/Telegram/build/version"
          VERSION=$(grep 'AppVersionStr ' "$VFILE" | awk '{ print $2 }')
          VERSION_NUM=$(grep 'AppVersion ' "$VFILE" | awk '{ print $2 }')
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "VERSION_NUM=$VERSION_NUM" >> $GITHUB_ENV
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "version_num=$VERSION_NUM" >> $GITHUB_OUTPUT

      - name: Apply patches
        shell: python3 {0}
        env:
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          import os, sys, re

          src = "tdesktop"
          patched = {}

          def patch(path, old, new):
              full = os.path.join(src, path)
              if not os.path.exists(full):
                  return
              with open(full, "r", encoding="utf-8") as f:
                  content = f.read()
              if old in content:
                  with open(full, "w", encoding="utf-8", newline="\n") as f:
                      f.write(content.replace(old, new))
                  patched.setdefault(path, []).append(old[:60])

          repo = os.environ.get("GITHUB_REPOSITORY", "RezoxP/AyuGramDesktop-builder")
          url = f"https://raw.githubusercontent.com/{repo}/update-manifest/"

          update_url_patterns = [
              ('return AutoupdatePrefix("https://update.ayugram.one/");',
               f'return AutoupdatePrefix("{url}");'),
              ('const auto fixedPrefix = QString::fromStdString("https://update.ayugram.one/");',
               f'const auto fixedPrefix = QString::fromStdString("{url}");'),
          ]
          t_rel = os.path.join("Telegram", "SourceFiles", "storage", "localstorage.cpp")
          for old, new in update_url_patterns:
              patch(t_rel, old, new)

          u_rel = os.path.join("Telegram", "SourceFiles", "core", "update_checker.cpp")
          patch(u_rel,
                'Local::readAutoupdatePrefix() + bestLink)',
                'bestLink.startsWith(QLatin1String("http")) ? bestLink : (Local::readAutoupdatePrefix() + bestLink))')

          required = {
              os.path.join(src, t_rel): 'AutoupdatePrefix',
              os.path.join(src, u_rel): 'bestLink.startsWith',
          }
          for fpath, marker in required.items():
              with open(fpath, "r", encoding="utf-8") as f:
                  if marker not in f.read():
                      print(f"FATAL: patch verification failed for {fpath} (missing {marker})")
                      sys.exit(1)

          for path, items in patched.items():
              print(f"Patched {path}: {len(items)} replacement(s)")

      - name: Enable parallel Docker build
        run: |
          sed -i 's|cmake --build \.\./out|cmake --build ../out --parallel $(nproc)|' \
            tdesktop/Telegram/build/docker/centos_env/build.sh

      - name: Build AyuGram
        run: |
          cd tdesktop
          docker run --rm \
            -u $(id -u) \
            -v "$PWD:/usr/src/tdesktop" \
            -e CONFIG=Release \
            ghcr.io/telegramdesktop/tdesktop/centos_env:latest \
            /usr/src/tdesktop/Telegram/build/docker/centos_env/build.sh \
            -D TDESKTOP_API_ID=${{ env.TDESKTOP_API_ID }} \
            -D TDESKTOP_API_HASH=${{ env.TDESKTOP_API_HASH }} \
            -D DESKTOP_APP_DISABLE_AUTOUPDATE=OFF \
            -D DESKTOP_APP_DISABLE_CRASH_REPORTS=ON

      - name: Package
        run: |
          mkdir -p pkg
          for bin in AyuGram Telegram; do
            [ -f "tdesktop/out/Release/$bin" ] && cp "tdesktop/out/Release/$bin" pkg/AyuGram && break
          done
          [ -f tdesktop/out/Release/Updater ] && cp tdesktop/out/Release/Updater pkg/
          strip --strip-all pkg/* 2>/dev/null || true
          chmod +x pkg/*
          tar -czf "AyuGram-Linux-x64-${VERSION}.tar.gz" -C pkg .

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: AyuGram-Linux-x64-${{ env.VERSION }}
          path: "AyuGram-Linux-x64-${{ env.VERSION }}.tar.gz"
          retention-days: 90

  release:
    name: Create Release
    needs: [windows, linux]
    if: success()
    runs-on: ubuntu-latest

    env:
      VERSION: ${{ needs.linux.outputs.version }}
      VERSION_NUM: ${{ needs.linux.outputs.version_num }}

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: release/
          merge-multiple: true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ env.VERSION }}
          name: AyuGram v${{ env.VERSION }}
          body: |
            **AyuGram Desktop v${{ env.VERSION }}**

            | Platform | Arch | File |
            |----------|------|------|
            | Windows | x64 | `AyuGram-Windows-x64-${{ env.VERSION }}.zip` |
            | Windows | x86 | `AyuGram-Windows-x86-${{ env.VERSION }}.zip` |
            | Linux | x64 | `AyuGram-Linux-x64-${{ env.VERSION }}.tar.gz` |

            Branch: `${{ github.event.inputs.branch }}`
          files: release/*
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Update manifest
        run: |
          TAG="v${VERSION}"
          BASE="https://github.com/${{ github.repository }}/releases/download/${TAG}"
          cat > current4 << EOF
          {
            "win64": { "stable": { "released": "${VERSION_NUM}", "link": "${BASE}/AyuGram-Windows-x64-${VERSION}.zip" } },
            "win":   { "stable": { "released": "${VERSION_NUM}", "link": "${BASE}/AyuGram-Windows-x86-${VERSION}.zip" } },
            "linux": { "stable": { "released": "${VERSION_NUM}", "link": "${BASE}/AyuGram-Linux-x64-${VERSION}.tar.gz" } }
          }
          EOF
          python3 -c "import json; json.load(open('current4'))" || { echo 'Invalid manifest JSON'; exit 1; }
          git init -q
          git remote add origin "https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git"
          git config user.email "AyuGramBuilder@users.noreply.github.com"
          git config user.name "AyuGram CI"
          git checkout --orphan update-manifest
          git add current4
          git commit -q -m "Update manifest for v${VERSION}"
          git push -f origin update-manifest
